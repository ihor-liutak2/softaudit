specVersion: "v1"
serviceId: "audit-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "audit-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
