import { Routes } from '@angular/router';
import { ValidationRootComponent } from './validation-root.component';

export const validationRoutes: Routes = [
  { path: '', component: ValidationRootComponent }
];
